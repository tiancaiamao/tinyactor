((import "str" "str") (define (worker) (match (recv) ((cons (quote data) lst) (print (str.concat (car lst) (car (cdr lst))))))) (define (main) (let msg (list "hello " "world") (let pid (spawn (lambda nil (worker) nil)) (begin (send pid (cons (quote data) msg)) (let ref (monitor pid)) (recv))))))
