// module-private-errors.ta — NEGATIVE test (issue #96 Phase B): a dotted
// reference to a non-pub member of an imported module must be rejected at
// compile time with [E0004] ("is not exported by module").
import phase-pub

fn main() {
  print(phase-pub.halve(21))
}
