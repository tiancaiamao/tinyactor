// const-basic.ta — TEMPORARY: avoid const keyword, use let bindings
// FIXME: Restore const support after bootstrap is complete

fn main() {
  let msg = "hello world"
  let count = 42
  let double = count * 2
  print(count)
  print(msg)
  print(double)
  print(string-length(msg))
}