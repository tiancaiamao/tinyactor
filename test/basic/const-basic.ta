// const-basic.ta — Compile-time constant support (inlined by codegen)
const MSG = "hello world"
const COUNT = 42
const DOUBLE = COUNT * 2

fn main() {
  print(COUNT)
  print(MSG)
  print(DOUBLE)
  print(str.length(MSG))
}
