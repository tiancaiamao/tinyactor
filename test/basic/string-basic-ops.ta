// Test: string-basic-ops
// Purpose: Test basic string operations via str module.
// Expected output: 5\nhello world

import str

fn main() {
  let s = "hello"
  print(str.length(s))
  print(str.concat(s, " world"))
}