// Test: string-basic-ops
// Purpose: Test basic string operations via str module.
// Expected output: 5\nhello world\nA\nBC

import str

fn main() {
  let s = "hello"
  print(str.length(s))
  print(str.concat(s, " world"))
  print(str.chr(65))
  print(str.concat(str.chr(66), str.chr(67)))
}