// Port of cora/test/t/basic-lambda.cora
// Original: (((lambda (x y) (lambda (z) y)) 1 2) 3)
// Nested lambda: outer captures y, inner closure returns captured y.
// Expected output: 2

fn main() {
  let f = fn(x, y) { fn(z) { y } }
  let g = f(1, 2)
  print(g(3))
}